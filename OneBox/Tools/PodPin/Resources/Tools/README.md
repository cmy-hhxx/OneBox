# Runtime tool resources

No external executable is bundled in the first milestone. Future packaged tools
must be declared in the repository-level `Tools/tool-lock.json` and copied here
only by a checksum-verifying build step.
